version: '3.8'

services:
  admin:
    container_name: vue-admin
    build: .
    ports:
      - 127.0.0.1:3001:3001
    environment:
      LETSENCRYPT_HOST: admin.zwd.xyz
      VIRTUAL_HOST: admin.zwd.xyz
      VIRTUAL_PORT: 3001
      VIRTUAL_PATH: /
    network_mode: bridge
